enum IdType {
  PASSPORT
  DRIVERS_LICENSE
  NATIONAL_ID
  VOTERS_ID
  OTHER
}

enum RiskRating {
  LOW
  MEDIUM
  HIGH
}

enum TransactionType {
  DEPOSIT
  WITHDRAWAL
  TRANSFER
  TRANSFER_INTERNAL
  PAYMENT
}

enum TransactionDirection {
  INCOMING
  OUTGOING
}